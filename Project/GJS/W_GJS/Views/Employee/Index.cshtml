@model IEnumerable<W_GJS.Models.M_EMPLOYEE>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/Admin/_AdminLayout.cshtml";
}

@section Breadcrumb {
    <ul class="breadcrumb breadcrumb-page">
        <li class="breadcrumb-label text-light-gray">Bạn đang ở đây: </li>
        <li><a href="#">Trang chủ</a></li>
        <li class="active"><a href="#">Nhân viên</a></li>
    </ul>

    <div class="page-header">

        <div class="row">
            <!-- Page header, center on small screens -->
            <h1 class="col-xs-12 col-sm-4 text-center text-left-sm"><i class="fa fa-dashboard page-header-icon"></i>&nbsp;&nbsp;Nhân viên</h1>
            <div class="col-xs-12 col-sm-8">
                <div class="row">
                    <hr class="visible-xs no-grid-gutter-h">
                    <!-- Margin -->
                    <div class="visible-xs clearfix form-group-margin"></div>
                </div>
            </div>
        </div>
    </div>
}

<div class="row">
    <div class="table-primary">
        <div class="table-header">
            <div class="table-caption">
                Danh sách
            </div>
        </div>
        <table class="table table-bordered table-hover table-striped">
            <thead>
                <tr>
                  
                    <th>
                        @Html.DisplayNameFor(model => model.EMPLOYEE_CODE)
                    </th>
                    <th>
                        @Html.DisplayNameFor(model => model.EMPLOYEE_NAME)
                    </th>
                    <th>
                        @Html.DisplayNameFor(model => model.ADDRESS)
                    </th>
                    <th>
                        @Html.DisplayNameFor(model => model.EMAIL)
                    </th>
                    <th>
                        @Html.DisplayNameFor(model => model.BIRTHDAY)
                    </th>
                  
                    <th></th>
                </tr>
            </thead>
            <tbody>
                @foreach (var item in Model)
                {
                    <tr>
                      
                        <td>
                            @Html.DisplayFor(modelItem => item.EMPLOYEE_CODE)
                        </td>
                        <td>
                            @Html.DisplayFor(modelItem => item.EMPLOYEE_NAME)
                        </td>
                        <td>
                            @Html.DisplayFor(modelItem => item.ADDRESS)
                        </td>
                        <td>
                            @Html.DisplayFor(modelItem => item.EMAIL)
                        </td>
                        <td>
                            @Html.DisplayFor(modelItem => item.BIRTHDAY)
                        </td>
                      
                        <td>
                            @*@Html.ActionLink("Sửa", "Edit", new { EMPLOYEE_CD = item.EMPLOYEE_CD }) |
                            @if (item.ACTIVE == false)
                            {
                                @Html.ActionLink("Kích hoạt", "Active", new { EMPLOYEE_CD = item.EMPLOYEE_CD })
                            }
                            else
                            {
                                @Html.ActionLink("Xóa", "Deactive", new { EMPLOYEE_CD = item.EMPLOYEE_CD })
                            }*@
                        </td>
                    </tr>
                }
            </tbody>
        </table>
        <div class="table-footer">

        </div>
    </div>
</div>

